# سیستم هوش مصنوعی تابش

## معرفی

سیستم هوش مصنوعی تابش یک دستیار هوشمند جامع برای کمک به مشتریان در ثبت سفارشات چاپ کتاب است. این سیستم با استفاده از Google Gemini AI، به صورت زنده به سوالات مشتریان پاسخ می‌دهد و راهنمایی‌های لازم را در مورد انتخاب مشخصات کتاب ارائه می‌کند.

## ویژگی‌ها

### 🤖 دستیار هوشمند تعاملی
- پاسخگویی به سوالات مشتریان در زمان واقعی
- راهنمایی در انتخاب مشخصات کتاب (قطع، نوع کاغذ، صحافی و...)
- تخمین قیمت و ارائه پیشنهادات
- خواندن و تحلیل اطلاعات فرم سفارش

### 🔧 سه حالت عملکرد
1. **Direct Mode (حالت مستقیم)**: اتصال مستقیم به Google Gemini API
2. **Server Mode (حالت سرور)**: ارائه سرویس AI به کلاینت‌های خارجی
3. **Client Mode (حالت کلاینت)**: اتصال به یک سرور خارجی

### 🔒 سیستم امنیتی پیشرفته
- کنترل دسترسی بر اساس نقش کاربری
- محدودیت دسترسی به داده‌های حساس
- احراز هویت با nonce برای تمام درخواست‌ها
- رمزنگاری ارتباطات

### 🎨 رابط کاربری زیبا و کاربرپسند
- طراحی واکنش‌گرا (Responsive) برای موبایل و دسکتاپ
- پشتیبانی کامل از RTL برای زبان فارسی
- انیمیشن‌های روان و حرفه‌ای
- دکمه شناور برای دسترسی سریع

## نصب و راه‌اندازی

### پیش‌نیازها
- وردپرس 6.8 یا بالاتر
- PHP 8.2.2 یا بالاتر
- افزونه تابش نصب و فعال شده
- حساب کاربری Google AI Studio (برای حالت Direct)

### مراحل راه‌اندازی

#### 1. دریافت کلید API از Google

1. به [Google AI Studio](https://aistudio.google.com/app/apikey) بروید
2. وارد حساب گوگل خود شوید
3. روی "Create API Key" کلیک کنید
4. کلید ایجاد شده را کپی کنید

#### 2. تنظیم افزونه

1. در پنل مدیریت وردپرس به **تابش > تنظیمات** بروید
2. تب **تنظیمات هوش مصنوعی** را انتخاب کنید
3. گزینه **فعالسازی هوش مصنوعی** را تیک بزنید
4. **حالت عملکرد** را روی **مستقیم (Direct)** قرار دهید
5. **کلید API گوگل Gemini** را وارد کنید
6. روی دکمه **تست اتصال** کلیک کنید تا اطمینان حاصل کنید
7. **نقش‌های مجاز** را انتخاب کنید (مثلاً: Administrator, Customer)
8. **کنترل دسترسی** را تنظیم کنید
9. روی **ذخیره تغییرات** کلیک کنید

#### 3. نمایش رابط گفتگو

برای نمایش دستیار هوشمند در صفحات خود، از شورتکد زیر استفاده کنید:

```
[tabesh_ai_chat]
```

این شورتکد را می‌توانید در:
- صفحه ثبت سفارش
- صفحات راهنما
- هر صفحه یا پستی که می‌خواهید

قرار دهید.

## استفاده

### برای مشتریان

1. در صفحه‌ای که شورتکد را قرار داده‌اید، یک دکمه شناور در گوشه چپ/راست پایین صفحه نمایش داده می‌شود
2. با کلیک روی دکمه، پنجره گفتگو باز می‌شود
3. سوال خود را بنویسید و ارسال کنید
4. دستیار هوشمند به سرعت پاسخ می‌دهد
5. از دکمه‌های پیشنهادی برای پرسش‌های متداول استفاده کنید

### برای مدیران

در پنل تنظیمات می‌توانید:

- **فعال/غیرفعال کردن** سیستم را کنترل کنید
- **حالت عملکرد** را تغییر دهید
- **نقش‌های مجاز** را مدیریت کنید
- **دسترسی به داده‌ها** را محدود کنید
- **تنظیمات کش** را بهینه کنید
- **پارامترهای AI** (temperature, max_tokens) را تنظیم کنید

## API Endpoints

### POST `/wp-json/tabesh/v1/ai/chat`

ارسال پیام به دستیار هوشمند

**Headers:**
```
Content-Type: application/json
X-WP-Nonce: [wp_rest_nonce]
```

**Request Body:**
```json
{
    "message": "قیمت چاپ یک کتاب چقدر است؟",
    "context": {
        "form_data": {
            "book_size": "وزیری",
            "paper_type": "گلاسه",
            "page_count": "200"
        }
    }
}
```

**Response:**
```json
{
    "success": true,
    "message": "قیمت چاپ بستگی به عوامل مختلفی دارد..."
}
```

### GET `/wp-json/tabesh/v1/ai/form-data`

دریافت اطلاعات فرم

**Headers:**
```
X-WP-Nonce: [wp_rest_nonce]
```

**Response:**
```json
{
    "success": true,
    "data": {
        "book_sizes": ["وزیری", "رقعی", "A4"],
        "paper_types": ["گلاسه", "تحریر"],
        "print_types": ["سیاه و سفید", "رنگی"],
        "binding_types": ["گالینگور", "سوئیزی"]
    }
}
```

### POST `/wp-json/tabesh/v1/ai/forward`

ارسال درخواست به سرور خارجی (برای حالت Client)

## تنظیمات پیشرفته

### Temperature

کنترل خلاقیت و تنوع پاسخ‌ها:
- **0.0-0.3**: پاسخ‌های دقیق و قابل پیش‌بینی
- **0.4-0.7**: تعادل بین دقت و خلاقیت (پیشنهادی)
- **0.8-2.0**: پاسخ‌های خلاقانه‌تر و متنوع‌تر

### Max Tokens

حداکثر تعداد توکن‌های خروجی:
- **256-512**: پاسخ‌های کوتاه
- **512-2048**: پاسخ‌های متوسط (پیشنهادی)
- **2048-8192**: پاسخ‌های بلند و تفصیلی

### کش

برای بهبود عملکرد و کاهش هزینه:
- **فعال بودن کش**: پاسخ‌های تکراری را کش می‌کند
- **مدت اعتبار**: مدت زمان نگهداری پاسخ‌ها (پیشنهادی: 3600 ثانیه)

## حالت‌های عملکرد

### Direct Mode (حالت مستقیم)

برای استفاده مستقیم از Google Gemini:

**مزایا:**
- سریع‌ترین حالت
- بدون نیاز به سرور واسط
- کنترل کامل بر تنظیمات

**نیازمندی‌ها:**
- کلید API از Google
- دسترسی به اینترنت

### Server Mode (حالت سرور)

برای ارائه سرویس به چند سایت:

**مزایا:**
- یک کلید API برای چند سایت
- مدیریت متمرکز
- کاهش هزینه‌ها

**نیازمندی‌ها:**
- سرور قدرتمند
- کلید API

### Client Mode (حالت کلاینت)

برای اتصال به یک سرور مرکزی:

**مزایا:**
- بدون نیاز به کلید API
- بدون نگرانی از هزینه‌های API
- به‌روزرسانی‌های متمرکز

**نیازمندی‌ها:**
- آدرس سرور
- کلید احراز هویت سرور

## امنیت

### کنترل دسترسی

سیستم دارای چهار سطح دسترسی است:

1. **دسترسی به سفارشات**: مشاهده اطلاعات سفارشات
2. **دسترسی به کاربران**: مشاهده اطلاعات کاربران
3. **دسترسی به قیمت‌گذاری**: مشاهده محاسبات قیمت
4. **دسترسی به ووکامرس**: مشاهده داده‌های فروشگاه

### احراز هویت

تمام درخواست‌های API با:
- WordPress Nonce
- کنترل دسترسی نقش‌محور (RBAC)
- بررسی مجوزهای سفارشی

محافظت می‌شوند.

## عیب‌یابی

### مشکلات متداول

#### 1. خطای "کلید API نامعتبر است"

**راه‌حل:**
- کلید API را دوباره بررسی کنید
- مطمئن شوید که کلید فعال است
- از [Google AI Studio](https://aistudio.google.com/app/apikey) کلید جدید دریافت کنید

#### 2. دستیار پاسخ نمی‌دهد

**راه‌حل:**
- از فعال بودن سیستم در تنظیمات اطمینان حاصل کنید
- نقش کاربری خود را بررسی کنید
- کنسول مرورگر را برای خطاها بررسی کنید

#### 3. خطای "دسترسی غیرمجاز"

**راه‌حل:**
- نقش‌های مجاز در تنظیمات را بررسی کنید
- مطمئن شوید که کاربر وارد شده است
- کش مرورگر را پاک کنید

## سوالات متداول (FAQ)

### آیا برای استفاده باید هزینه پرداخت کنم؟

Google Gemini یک سهمیه رایگان در ماه ارائه می‌دهد. برای استفاده‌های بیشتر، نیاز به پرداخت دارید.

### آیا اطلاعات مشتریان امن است؟

بله، سیستم دارای کنترل دسترسی دقیق است و شما می‌توانید تعیین کنید که چه اطلاعاتی در دسترس AI قرار گیرد.

### آیا می‌توان زبان دیگری غیر از فارسی استفاده کرد؟

بله، Gemini از زبان‌های مختلف پشتیبانی می‌کند. سیستم را برای زبان دلخواه پیکربندی کنید.

### چگونه می‌توانم عملکرد را بهبود دهم؟

- کش را فعال کنید
- مقدار max_tokens را کاهش دهید
- از مدل Flash به جای Pro استفاده کنید

## پشتیبانی

برای پشتیبانی و گزارش مشکلات:

- **ایمیل**: support@chapco.ir
- **وب‌سایت**: https://chapco.ir

## مجوز

این افزونه تحت مجوز GPL v2 یا بالاتر منتشر شده است.

---

**نسخه**: 1.0.0  
**آخرین به‌روزرسانی**: 2025-01-24
